import "./home.scss";
import React from "react";
import { Header } from "components";

const Home = () => {
  return (
    <main className="home">
      <Header />
    </main>
  );
};

export default Home;
